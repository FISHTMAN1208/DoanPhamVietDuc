<Window x:Class="DoanPhamVietDuc.Views.Categories.AddCategoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:DoanPhamVietDuc.Views.Categories"
        mc:Ignorable="d"
        Title="Thêm danh mục mới" 
        Height="350" 
        Width="500"
        WindowStartupLocation="CenterScreen"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        Background="#F8FAFC">

    <Window.Resources>
        <Style x:Key="MaterialDesignOutlinedTextBoxWithBorder" 
               BasedOn="{StaticResource MaterialDesignOutlinedTextBox}" 
               TargetType="TextBox">
            <Setter Property="materialDesign:TextFieldAssist.UnderlineBrush" Value="#1E293B"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#6366F1"/>
                    <Setter Property="BorderThickness" Value="1"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="MaterialDesignOutlinedDatePickerWithBorder" 
               BasedOn="{StaticResource MaterialDesignOutlinedDatePicker}" 
               TargetType="DatePicker">
            <Setter Property="materialDesign:TextFieldAssist.UnderlineBrush" Value="#1E293B"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#6366F1"/>
                    <Setter Property="BorderThickness" Value="1"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#1E293B" Padding="15" Margin="0,0,0,20" CornerRadius="5">
            <DockPanel LastChildFill="True">
                <materialDesign:PackIcon Kind="FolderPlus" 
                                         Width="24" 
                                         Height="24" 
                                         Foreground="White" 
                                         VerticalAlignment="Center"/>
                <TextBlock Text="THÊM DANH MỤC MỚI" 
                           FontSize="18" 
                           FontWeight="Bold" 
                           Foreground="White" 
                           Margin="10,0,0,0" 
                           VerticalAlignment="Center"/>
            </DockPanel>
        </Border>
        
        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <!-- Tên danh mục -->
            <TextBlock Text="Tên danh mục" FontWeight="Medium" Margin="0,0,0,5"/>
            <TextBox Style="{StaticResource MaterialDesignOutlinedTextBoxWithBorder}"
                     Text="{Binding Category.CategoryName, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,15"/>

            <!-- Người thêm -->
            <TextBlock Text="Người thêm" FontWeight="Medium" Margin="0,0,0,5"/>
            <TextBox Style="{StaticResource MaterialDesignOutlinedTextBoxWithBorder}"
                     Text="{Binding Category.ModifyBy, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,15"/>

            <!-- Thời gian cập nhật -->
            <TextBlock Text="Thời gian cập nhật" FontWeight="Medium" Margin="0,0,0,5"/>
            <DatePicker Style="{StaticResource MaterialDesignOutlinedDatePickerWithBorder}"
                        SelectedDate="{Binding Category.ModifyTime}"
                        IsEnabled="False"
                        Margin="0,0,0,0"/>
        </StackPanel>

        <!-- Footer -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Hủy bỏ"
                    Command="{Binding CancelCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Background="#EF4444"
                    Foreground="White"
                    Width="100"
                    Height="36"
                    Margin="0,0,10,0"/>

            <Button Content="Thêm"
                    Command="{Binding SaveCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Background="#10B981"
                    Foreground="White"
                    Width="100"
                    Height="36"/>
        </StackPanel>
    </Grid>
</Window>