<Window x:Class="DoanPhamVietDuc.Views.Books.AddBookWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:DoanPhamVietDuc.Views.Books"
        mc:Ignorable="d"
        Title="Thêm sách mới" 
        Height="975" 
        Width="1370"
        WindowStartupLocation="CenterScreen"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        Background="#F8FAFC">

    <Window.Resources>
        <!-- Style để luôn hiển thị viền ngoài khi TextBox có focus -->
        <Style x:Key="MaterialDesignOutlinedTextBoxWithBorder" 
               BasedOn="{StaticResource MaterialDesignOutlinedTextBox}" 
               TargetType="TextBox">
            <Setter Property="materialDesign:TextFieldAssist.UnderlineBrush" Value="#1E293B"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#6366F1"/>
                    <Setter Property="BorderThickness" Value="1"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Style để luôn hiển thị viền ngoài khi ComboBox có focus -->
        <Style x:Key="MaterialDesignOutlinedComboBoxWithBorder" 
               BasedOn="{StaticResource MaterialDesignOutlinedComboBox}" 
               TargetType="ComboBox">
            <Setter Property="materialDesign:TextFieldAssist.UnderlineBrush" Value="#1E293B"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#6366F1"/>
                    <Setter Property="BorderThickness" Value="1"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Style để luôn hiển thị viền ngoài khi DatePicker có focus -->
        <Style x:Key="MaterialDesignOutlinedDatePickerWithBorder" 
               BasedOn="{StaticResource MaterialDesignOutlinedDatePicker}" 
               TargetType="DatePicker">
            <Setter Property="materialDesign:TextFieldAssist.UnderlineBrush" Value="#1E293B"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#6366F1"/>
                    <Setter Property="BorderThickness" Value="1"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <!-- Header -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#1E293B" Padding="20,15">
            <DockPanel LastChildFill="True">
                <materialDesign:PackIcon Kind="BookPlus" 
                                         Width="32" 
                                         Height="32" 
                                         Foreground="White" 
                                         VerticalAlignment="Center"/>
                <TextBlock Text="THÊM SÁCH MỚI" 
                           FontSize="22" 
                           FontWeight="Bold" 
                           Foreground="White" 
                           Margin="15,0,0,0" 
                           VerticalAlignment="Center"/>
            </DockPanel>
        </Border>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <!-- Left Column - Form Fields -->
                <Grid Grid.Column="0" Margin="0,0,20,0">
                    <!-- Group: Thông tin cơ bản -->
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <!-- Thêm row cho thông tin người thêm -->
                    </Grid.RowDefinitions>

                    <!-- Group 1: Thông tin cơ bản -->
                    <GroupBox Grid.Row="0" 
                              Header="Thông tin cơ bản" 
                              Margin="0,0,0,15" 
                              FontSize="16"
                              FontWeight="Bold"
                              Style="{StaticResource MaterialDesignGroupBox}">
                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Tiêu đề -->
                            <TextBox Grid.Row="0" 
                                     Grid.Column="0"
                                     Grid.ColumnSpan="2"
                                     Style="{StaticResource MaterialDesignOutlinedTextBoxWithBorder}"
                                     materialDesign:HintAssist.Hint="Tiêu đề sách"
                                     Text="{Binding Book.Title, UpdateSourceTrigger=PropertyChanged}"
                                     Margin="0,0,0,15"/>

                            <!-- Tác giả -->
                            <TextBox Grid.Row="1" 
                                     Grid.Column="0"
                                     Style="{StaticResource MaterialDesignOutlinedTextBoxWithBorder}"
                                     materialDesign:HintAssist.Hint="Tác giả"
                                     Text="{Binding Book.Author, UpdateSourceTrigger=PropertyChanged}"
                                     Margin="0,0,10,15"/>

                            <!-- Thể loại -->
                            <ComboBox Grid.Row="1" 
                                      Grid.Column="1"
                                      Style="{StaticResource MaterialDesignOutlinedComboBoxWithBorder}"
                                      materialDesign:HintAssist.Hint="Thể loại"
                                      ItemsSource="{Binding Categories}"
                                      SelectedValue="{Binding Book.CategoryID}"
                                      SelectedValuePath="CategoryID"
                                      DisplayMemberPath="CategoryName"
                                      Margin="0,0,0,15"/>

                            <!-- Ngôn ngữ -->
                            <ComboBox Grid.Row="2" 
                                      Grid.Column="0"
                                      Style="{StaticResource MaterialDesignOutlinedComboBoxWithBorder}"
                                      materialDesign:HintAssist.Hint="Ngôn ngữ"
                                      ItemsSource="{Binding Languages}"
                                      SelectedValue="{Binding Book.LanguageID}"
                                      SelectedValuePath="LanguageID"
                                      DisplayMemberPath="LanguageName"
                                      Margin="0,0,10,15"/>

                            <!-- Loại bìa -->
                            <ComboBox Grid.Row="2" 
                                      Grid.Column="1"
                                      Style="{StaticResource MaterialDesignOutlinedComboBoxWithBorder}"
                                      materialDesign:HintAssist.Hint="Loại bìa"
                                      ItemsSource="{Binding BookCoverTypes}"
                                      SelectedValue="{Binding Book.BookCoverTypeID}"
                                      SelectedValuePath="BookCoverTypeID"
                                      DisplayMemberPath="BookCoverTypeName"
                                      Margin="0,0,0,15"/>

                            <!-- Mã ISBN -->
                            <TextBox Grid.Row="3" 
                                     Grid.Column="0"
                                     Style="{StaticResource MaterialDesignOutlinedTextBoxWithBorder}"
                                     materialDesign:HintAssist.Hint="Mã ISBN"
                                     Text="{Binding Book.ISBNCode, UpdateSourceTrigger=PropertyChanged}"
                                     Margin="0,0,10,0"/>

                            <!-- Số trang -->
                            <TextBox Grid.Row="3" 
                                     Grid.Column="1"
                                     Style="{StaticResource MaterialDesignOutlinedTextBoxWithBorder}"
                                     materialDesign:HintAssist.Hint="Số trang"
                                     Text="{Binding Book.PageCount, UpdateSourceTrigger=PropertyChanged}"
                                     Margin="0,0,0,0"/>
                        </Grid>
                    </GroupBox>

                    <!-- Group 2: Thông tin xuất bản -->
                    <GroupBox Grid.Row="1" 
                              Header="Thông tin xuất bản" 
                              FontSize="16"
                              FontWeight="Bold"
                              Margin="0,0,0,15" 
                              Style="{StaticResource MaterialDesignGroupBox}">
                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Nhà xuất bản -->
                            <TextBox Grid.Row="0" 
                                     Grid.Column="0"
                                     Style="{StaticResource MaterialDesignOutlinedTextBoxWithBorder}"
                                     materialDesign:HintAssist.Hint="Nhà xuất bản"
                                     Text="{Binding Book.PublisherName, UpdateSourceTrigger=PropertyChanged}"
                                     Margin="0,0,10,15"/>

                            <!-- Nhà cung cấp -->
                            <ComboBox Grid.Row="0" 
                                      Grid.Column="1"
                                      Style="{StaticResource MaterialDesignOutlinedComboBoxWithBorder}"
                                      materialDesign:HintAssist.Hint="Nhà cung cấp"
                                      ItemsSource="{Binding Suppliers}"
                                      SelectedValuePath="SupplierID"
                                      SelectedValue="{Binding Book.SupplierID}"
                                      DisplayMemberPath="SupplierName"
                                      Margin="0,0,0,15"/>

                            <!-- Thời gian xuất bản -->
                            <DatePicker Grid.Row="1" 
                                        Grid.Column="0"
                                        Style="{StaticResource MaterialDesignOutlinedDatePickerWithBorder}"
                                        materialDesign:HintAssist.Hint="Ngày xuất bản"
                                        SelectedDate="{Binding Book.PublishTime}"
                                        Margin="0,0,10,0"/>
                        </Grid>
                    </GroupBox>

                    <!-- Group 3: Thông tin kinh doanh -->
                    <GroupBox Grid.Row="2" 
                              Header="Thông tin kinh doanh" 
                              FontSize="16"
                              FontWeight="Bold"
                              Margin="0,0,0,15"
                              Style="{StaticResource MaterialDesignGroupBox}">
                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Giá bán -->
                            <TextBox Grid.Row="0" 
                                     Grid.Column="0"
                                     Style="{StaticResource MaterialDesignOutlinedTextBoxWithBorder}"
                                     materialDesign:HintAssist.Hint="Giá bán (VNĐ)"
                                     Text="{Binding Book.Price, UpdateSourceTrigger=PropertyChanged}"
                                     Margin="0,0,10,15"/>

                            <!-- Số lượng -->
                            <TextBox Grid.Row="0" 
                                     Grid.Column="1"
                                     Style="{StaticResource MaterialDesignOutlinedTextBoxWithBorder}"
                                     materialDesign:HintAssist.Hint="Số lượng"
                                     Text="{Binding Book.Quantity, UpdateSourceTrigger=PropertyChanged}"
                                     Margin="0,0,0,15"/>

                            <!-- Mô tả -->
                            <TextBox Grid.Row="1" 
                                     Grid.Column="0"
                                     Grid.ColumnSpan="2"
                                     Style="{StaticResource MaterialDesignOutlinedTextBoxWithBorder}"
                                     materialDesign:HintAssist.Hint="Mô tả"
                                     Text="{Binding Book.Description, UpdateSourceTrigger=PropertyChanged}"
                                     TextWrapping="Wrap"
                                     AcceptsReturn="True"
                                     VerticalAlignment="Stretch"
                                     Height="100"
                                     Margin="0,0,0,0"/>
                        </Grid>
                    </GroupBox>

                    <!-- Group 4: Thông tin cập nhật (Mới thêm) -->
                    <GroupBox Grid.Row="3"
                              Header="Thông tin cập nhật" 
                              FontSize="16"
                              FontWeight="Bold"
                              Style="{StaticResource MaterialDesignGroupBox}">
                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Người thêm -->
                            <TextBox Grid.Row="0" 
                                     Grid.Column="0"
                                     Style="{StaticResource MaterialDesignOutlinedTextBoxWithBorder}"
                                     materialDesign:HintAssist.Hint="Người thêm"
                                     Text="{Binding Book.ModifyBy, UpdateSourceTrigger=PropertyChanged}"
                                     Margin="0,0,10,15"/>

                            <!-- Thời gian cập nhật -->
                            <DatePicker Grid.Row="0" 
                                        Grid.Column="1"
                                        Style="{StaticResource MaterialDesignOutlinedDatePickerWithBorder}"
                                        materialDesign:HintAssist.Hint="Thời gian cập nhật"
                                        SelectedDate="{Binding Book.ModifyTime}"
                                        Margin="0,0,0,15"/>
                        </Grid>
                    </GroupBox>
                </Grid>

                <!-- Right Column - Image Preview -->
                <Grid Grid.Column="1">
                    <GroupBox Header="Ảnh bìa sách" FontSize="16" FontWeight="Bold" Style="{StaticResource MaterialDesignGroupBox}">
                        <StackPanel Margin="0,10,0,0">
                            <Border Width="220" 
                                    Height="300" 
                                    BorderThickness="1" 
                                    BorderBrush="#E2E8F0" 
                                    CornerRadius="5" 
                                    Background="#F8FAFC" 
                                    Margin="0,0,0,15"
                                    HorizontalAlignment="Center">
                                <Grid>
                                    <Image Source="{Binding ImagePreview}" 
                                           Stretch="Uniform" 
                                           Margin="10"/>
                                    <TextBlock Text="Chưa có ảnh" 
                                               HorizontalAlignment="Center" 
                                               VerticalAlignment="Center" 
                                               Visibility="{Binding HasNoImage, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                </Grid>
                            </Border>

                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBox Grid.Column="0"
                                         Style="{StaticResource MaterialDesignOutlinedTextBoxWithBorder}"
                                         materialDesign:HintAssist.Hint="Đường dẫn ảnh"
                                         Text="{Binding Book.ImageUrl, UpdateSourceTrigger=PropertyChanged}"
                                         IsReadOnly="True"
                                         Margin="0,0,10,0"/>

                                <Button Grid.Column="1"
                                        Content="Chọn ảnh"
                                        Command="{Binding BrowseImageCommand}"
                                        Style="{StaticResource MaterialDesignRaisedButton}"
                                        Background="#6366F1"
                                        Foreground="White"/>
                            </Grid>
                        </StackPanel>
                    </GroupBox>
                </Grid>
            </Grid>
        </ScrollViewer>

        <!-- Footer with buttons -->
        <Grid Grid.Row="2" Background="#F1F5F9" >
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Hủy bỏ"
                        Command="{Binding CancelCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Background="#EF4444"
                        Foreground="White"
                        Width="120"
                        Height="40"
                        Margin="0,0,15,0"/>

                <Button Content="Thêm sách"
                        Command="{Binding SaveCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Background="#10B981"
                        Foreground="White"
                        Width="120"
                        Height="40"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>