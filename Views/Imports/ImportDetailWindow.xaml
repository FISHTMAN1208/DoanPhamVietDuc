<Window x:Class="DoanPhamVietDuc.Views.Imports.ImportDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DoanPhamVietDuc.Views.Imports"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Chi tiết phiếu nhập" 
        Height="700" 
        Width="1000"
        WindowStartupLocation="CenterScreen"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#1E293B" Padding="15">
            <DockPanel>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="FileDocumentCheck" Width="32" Height="32" Foreground="White"/>
                    <TextBlock Text="CHI TIẾT PHIẾU NHẬP" FontSize="22" FontWeight="Bold" Foreground="White" Margin="15,0,0,0"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Thông tin phiếu nhập -->
        <Border Grid.Row="1" Background="#F1F5F9" CornerRadius="5" Padding="15" Margin="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Cột 1 -->
                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,15,0">
                    <TextBlock Text="Mã phiếu:" FontWeight="Bold"/>
                    <TextBlock Text="{Binding ImportID}" Margin="0,5,0,15"/>

                    <TextBlock Text="Nhà cung cấp:" FontWeight="Bold"/>
                    <TextBlock Text="{Binding Supplier.SupplierName}" Margin="0,5,0,0"/>
                </StackPanel>

                <!-- Cột 2 -->
                <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,15,0">
                    <TextBlock Text="Ngày nhập:" FontWeight="Bold"/>
                    <TextBlock Text="{Binding ImportDate, StringFormat='{}{0:dd/MM/yyyy}'}" Margin="0,5,0,15"/>

                    <TextBlock Text="Trạng thái:" FontWeight="Bold"/>
                    <TextBlock Text="{Binding ImportStatus}" Margin="0,5,0,0" FontWeight="SemiBold">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ImportStatus}" Value="Hoàn thành">
                                        <Setter Property="Foreground" Value="#10B981" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding ImportStatus}" Value="Đang xử lý">
                                        <Setter Property="Foreground" Value="#F59E0B" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding ImportStatus}" Value="Hủy bỏ">
                                        <Setter Property="Foreground" Value="#EF4444" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>

                <!-- Cột 3 -->
                <StackPanel Grid.Row="0" Grid.Column="2">
                    <TextBlock Text="Người tạo:" FontWeight="Bold"/>
                    <TextBlock Text="{Binding CreateBy}" Margin="0,5,0,15"/>

                    <TextBlock Text="Thời gian tạo:" FontWeight="Bold"/>
                    <TextBlock Text="{Binding CreateTime, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}" Margin="0,5,0,0"/>
                </StackPanel>

                <!-- Ghi chú -->
                <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,15,0,0">
                    <TextBlock Text="Ghi chú:" FontWeight="Bold"/>
                    <Border Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,5,0,0">
                        <TextBlock Text="{Binding Notes}" TextWrapping="Wrap"/>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Chi tiết phiếu nhập -->
        <Border Grid.Row="2" Background="White" CornerRadius="5" Padding="10" Margin="10" BorderBrush="#E2E8F0" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Danh sách sách nhập" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

                <DataGrid Grid.Row="1" 
                          ItemsSource="{Binding ImportDetails}" 
                          AutoGenerateColumns="False" 
                          IsReadOnly="True" 
                          Style="{StaticResource MaterialDesignDataGrid}"
                          CanUserAddRows="False"
                          VerticalScrollBarVisibility="Auto"
                          materialDesign:DataGridAssist.CellPadding="8">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="STT" Width="80">
                            <DataGridTextColumn.Binding>
                                <MultiBinding StringFormat="{}{0}">
                                    <Binding RelativeSource="{RelativeSource FindAncestor, AncestorType={x:Type DataGridRow}}" Path="Header"/>
                                </MultiBinding>
                            </DataGridTextColumn.Binding>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Sách" Binding="{Binding Book.Title}" Width="*"/>
                        <DataGridTextColumn Header="Mã ISBN" Binding="{Binding Book.ISBNCode}" Width="180"/>
                        <DataGridTextColumn Header="Tác giả" Binding="{Binding Book.Author}" Width="160"/>
                        <DataGridTextColumn Header="Số lượng" Binding="{Binding Quantity}" Width="120"/>

                        <DataGridTextColumn Header="Đơn giá" Width="120">
                            <DataGridTextColumn.Binding>
                                <Binding Path="UnitImportPrice" StringFormat="{}{0:N0} VNĐ" />
                            </DataGridTextColumn.Binding>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Thành tiền" Width="130">
                            <DataGridTextColumn.Binding>
                                <Binding Path="Subtotal" StringFormat="{}{0:N0} VNĐ" />
                            </DataGridTextColumn.Binding>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Footer - Tổng tiền và nút Đóng -->
        <Grid Grid.Row="3" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Tổng tiền -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left">
                <TextBlock Text="Tổng tiền:" 
                           FontSize="16" 
                           FontWeight="Bold" 
                           VerticalAlignment="Center"
                           Margin="0,0,15,0"/>
                <Border Background="#F1F5F9" 
                        Padding="15,10" 
                        CornerRadius="5" 
                        MinWidth="200">
                    <TextBlock Text="{Binding TotalAmount, StringFormat='{}{0:N0} VNĐ'}" 
                               FontSize="16" 
                               FontWeight="Bold" 
                               HorizontalAlignment="Left"/>
                </Border>
            </StackPanel>

            <!-- Nút Đóng -->
            <Button Grid.Column="1"
                    Content="Đóng"
                    Command="{Binding CloseCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Background="#64748B"
                    Foreground="White"
                    Width="100"
                    Height="36"/>
        </Grid>
    </Grid>
</Window>